# Minimal requirements for Railway deployment
# Only includes packages actually imported in backend_server.py

# Backend API Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Environment & Configuration
python-dotenv==1.0.1

# Database & Storage
supabase==2.9.0

# Additional tools
requests==2.32.3
aiohttp==3.10.10
